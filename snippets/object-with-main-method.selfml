(name [object with main method])

(trigger
    (tab omain))

(only-in source.scala)

(snippet [object %{1="%{TM_FILENAME/(.*)\.scala/$1/}"} {
  def main(args: Array[String]): Unit = {
    %{2}
  }
}
])